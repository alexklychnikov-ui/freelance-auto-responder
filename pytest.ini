[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -m "not evidence_baseline"
markers =
    evidence_baseline: intentional evidence baseline gaps; run with: pytest -o addopts= -m evidence_baseline
